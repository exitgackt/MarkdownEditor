import { Box, Typography } from '@mui/material';

const PricingPage = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="body1" color="text.secondary">
        この機能は現在準備中です（Phase 11で実装予定）
      </Typography>
    </Box>
  );
};

export default PricingPage;
